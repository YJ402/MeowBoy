using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEngine;

public abstract class _SceneManager : MonoBehaviour
{
    //[SerializeField] private GameObject SceneUIPrefab;
    //protected GameObject currentSceneUI;

    //protected virtual void Start()
    //{
    //    if (SceneUIPrefab != null)
    //    {
    //        currentSceneUI = Instantiate(SceneUIPrefab,transform);
    //    }
    //}
}
